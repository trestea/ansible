# {{ansible_managed}}

export MESOS_MASTER="{{ mesos_zookeeper_masters }}"
export MESOS_CONTAINERIZERS="{{ mesos_containerizers }}"
export MESOS_EXECUTOR_REGISTRATION_TIMEOUT="{{ mesos_executor_registration_timeout }}"
export MESOS_DOCKER_REMOVE_DELAY="{{ mesos_docker_remove_delay }}"
export MESOS_PORT="{{ mesos_slave_port }}"
export MESOS_WORK_DIR="{{ mesos_work_dir }}"
export MESOS_HOSTNAME="{{ mesos_hostname }}"
